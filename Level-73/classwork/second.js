// ობიექტი ორი key-value წყვილით
const numbers = {
    რიგი_0: [2, 4, 6, 8, 10], // ლუწი რიცხვები
    რიგი_1: [1, 3, 5, 7, 9],  // კენტი რიცხვები
  };
  
  // ერთდროული გადავლა
  for (let i = 0; i < numbers.რიგი_0.length; i++) {
    console.log(`ლუწი: ${numbers.რიგი_0[i]}, კენტი: ${numbers.რიგი_1[i]}`);
  }
  