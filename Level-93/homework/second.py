# საწყისი სიი (list)
numbers = [1, 2, 3, 4, 5]

# map-ის გამოყენება, სადაც თითოეულ ელემენტს ვამრავლებთ ორზე
result = list(map(lambda x: x * 2, numbers))

# შედეგის პრინტერება
print(result)  # გამოიტანს: [2, 4, 6, 8, 10]
