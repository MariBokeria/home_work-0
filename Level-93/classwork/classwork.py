# საწყისი სია
numbers = [1, 2, 3, 4, 5]

# map-ის გამოყენება, რათა თითოეულ ელემენტს დავუმატოთ 10
result = list(map(lambda x: x + 10, numbers))

# შედეგის პრინტერება
print(result)  # გამოიტანს: [11, 12, 13, 14, 15]
